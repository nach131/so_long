{
	// Use IntelliSense para saber los atributos posibles.
	// Mantenga el puntero para ver las descripciones de los existentes atributos.
	// Para más información, visite: https://go.microsoft.com/fwlink/?linkid=830387
	"version": "0.2.0",
	"configurations": [
		{
      "type": "lldb",
      "request": "launch",
      "name": "a.out",
      "program": "${fileDirname}/a.out",
      // "program": "${fileDirname}/${fileBasenameNoExtension}.out",
      // "args": ["min.ber"],
      "stopOnEntry": false,
      "cwd": "${fileDirname}"
    },
    {
      "type": "lldb",
      "request": "launch",
      "name": "so_long",
      "program": "${workspaceFolder}/sources/so_long",
      "args": ["${workspaceFolder}/sources/maps/min.ber"],
      // "args": ["${workspaceFolder}/sources/maps/fail/_no_player.ber"],
      // "args": ["${workspaceFolder}/sources/maps/fail/multi_player.ber"],
      "stopOnEntry": false,
      "cwd": "${fileDirname}"
    }
	]
}